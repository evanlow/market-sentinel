name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: pip install -e ".[dev]"
      - name: Run Ruff
        id: ruff
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          ruff check . --output-format=concise | tee ruff-report.txt
      - name: Prepare Ruff auto-fix patch
        if: steps.ruff.outcome == 'failure'
        run: |
          ruff check . --fix
          git diff -- . ':!.github/workflows/ci.yml' > ruff-fixes.patch
      - name: Upload Ruff diagnostics
        if: steps.ruff.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: ruff-diagnostics-${{ matrix.python-version }}
          path: |
            ruff-report.txt
            ruff-fixes.patch
      - run: pytest --cov=market_sentinel --cov-report=term-missing
      - name: Fail when Ruff found violations
        if: always() && steps.ruff.outcome == 'failure'
        run: exit 1
